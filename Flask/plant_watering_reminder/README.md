# Plant Watering Reminder

Small Flask app for tracking houseplants in `plants.json`, calculating the next watering date, and showing due or overdue plants first on the dashboard.

## Run

```powershell
pip install -r requirements.txt
python app.py
```

Open `http://127.0.0.1:5000/`.
